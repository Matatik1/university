def all_variants(text):
    for i in range(len(text)):
        for j in range(i+1,len(text)+1):
            result = text[i:j]
            yield result

a = all_variants("abc")
for i in a:
    print(i)
